/** Browser bundle entry for admin agent markdown (see `npm run vendor:comark`). */
export { createHtmlRenderer } from '@comark/html';
export { default as breaks } from '@comark/html/plugins/breaks';
export { default as security } from '@comark/html/plugins/security';
